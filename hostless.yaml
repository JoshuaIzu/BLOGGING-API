name: blogging-api-izu

build:
  system: nixpacks

start:
  command: yarn start

env:
  NODE_ENV: production

healthcheck:
  path: /
  interval: 30
  timeout: 10
  retries: 3

resources:
  memory: 512
  cpu: 0.5

scaling:
  min: 1
  max: 3

port: ${PORT}